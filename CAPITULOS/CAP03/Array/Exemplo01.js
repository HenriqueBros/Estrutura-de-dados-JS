let daysOfWeek = [];

daysOfWeek = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thurday', 'Friday', 'Saturday'];

console.log(daysOfWeek.length);

for(let i = 0; i < daysOfWeek.length; i++ ) {
    console.log(daysOfWeek[i]);
}